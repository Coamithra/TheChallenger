# Local settings - project paths and model choices are per machine.
challenger.conf

# Your own copy of the editor prompt, so editing the register never conflicts.
*.local.md

# Decision log: contains excerpts of your own sessions across every enabled project.
hook-debug.log

# Working notes and harvested source material, not part of the distribution.
research/

__pycache__/
*.pyc
